# 데모 (DEMO) — 교통 "양보/우회" 장면 재현

콘솔 UI에서 **로봇이 서로 양보하고 우회하는 모습**을 녹화용으로 *결정적으로* 연출하는 방법.
패트롤·랜덤 배차로는 교착이 우연에 의존해 재현이 어렵다 → `scripts/demo_yield.py`가
우선순위를 고정해 **매번 같은 장면**을 만든다.

---

## 0. 전제

| 필요 | 확인 |
|---|---|
| 스택 기동 | `./run_sim.sh` (gazebo·fleet·console 한 세션) |
| 콘솔 열림 | http://localhost:8001 (브라우저 자동 오픈) |
| 로봇 | slotcar 2대 이상 (`pinky1`, `pinky2`, …) |

> 스크립트는 **콘솔 :8001 HTTP만** 쓴다 → ROS 소싱·별도 설치 불필요(표준 라이브러리만).
> 빌드/토대 준비는 [`RUN.md`](RUN.md) 참고.

---

## 1. 맵 구조 (navgraph L1)

```
v0 —— v1 —— v2 —— v3     상단 통로 (y=9.8)   x: -6.8 · -3.8 · -1.2 · 0.6
 |     |     |     |
v4 —— v5 —— v6 —— v7     하단 통로 (y=3.2)
patrol_route = 0 1 2 3 7 6 5 4  (외곽을 같은 방향으로 순회)
```

---

## 2. 왜 교착이 우연히는 잘 안 나오나

1. **순회는 전부 같은 방향 루프** — 앞차를 따라갈 뿐 head-on 사이클이 안 생김. 기껏해야 뒤차가 잠깐 대기.
2. **노드예약이 정면충돌을 미리 차단** — "타깃 노드 먼저 확보 후 출발 노드 release" 규율이라 대부분 조용한 WAIT로 끝남.
3. **타이밍** — 두 로봇이 *같은 노드*를 *같은 순간*에 원해야 하는데 랜덤 배차로는 정렬이 거의 안 맞음.

→ **연출(choreography) + 우선순위 고정**이 답. 그래서 스크립트로 한다.

---

## 3. "양보"의 실제 메커니즘 (`reservation_deadlock.cpp`)

핵심 구분 — **단순 점유는 우회가 아니라 대기다.**

| 상황 | 결과 | UI |
|---|---|---|
| 상대가 노드를 **가만히 점유** | 나는 그 자리서 **WAIT(양보 대기)** — 우회 안 함 | 목표 노드가 상대 색으로 펄스, 나는 정지 |
| **head-on 사이클**(서로의 노드를 원함) | 낮은 우선순위가 **DEADLOCK→우회** | 진 로봇이 경로를 바꿔 아래로 돌아감 |
| 상대가 **STOP**(영구 장애물) | 지나가려던 내가 **우회** | 정지 로봇을 피해 새 경로 |

우선순위(`compute_priority`)는 **"사이클 안에서 누가 양보할지"만** 결정한다.
tier 서열: `순회(0) < 작업(1) < 충전복귀 CHARGE(2) < 완전막힘 STUCK(3)` · 동tier면 오래된 task > 낮은 배터리.

> ⚠️ **배터리로 우선순위 조작 금지** — 너무 낮추면 완주 관문(`insufficient_battery`)에서 task가 거절됨.
> 그래서 스크립트는 **CHARGE tier**로 "안 비키는 로봇"을 고정한다.

---

## 4. 실행 — 세 가지 씬

```bash
# ── 씬 1) 우선순위 양보 (head-on) ── ★기본·추천
#  상단 복도에서 두 로봇이 마주침 → 낮은 우선순위가 v5→v4로 우회, CHARGE 로봇은 직진 통과.
#  ('v0 점유 / v1→v0 / v5→v4' 세 역할이 이 한 장면에 다 나옴)
python3 scripts/demo_yield.py demo

# ── 씬 2) 정지 장애물 우회 (STOP) ──
#  boss를 v6에 STOP으로 세워둠(붙박이 점유) → other가 v5→v7 가다 v6을 피해 위로 우회.
#  "누가 노드 붙잡고 있어서 내가 돌아간다"는 그림 그대로.
python3 scripts/demo_yield.py obstacle

# ── 씬 3) 단순 양보(대기) ──
#  뒤 로봇이 앞 로봇 뒤 노드에서 ⏸ 멈춰 '양보 대기'(펄스). 우회는 없음.
python3 scripts/demo_yield.py queue

# ── 유틸 ──
python3 scripts/demo_yield.py reset          # 전원 IDLE (초기화만)
python3 scripts/demo_yield.py demo --loop    # 녹화 중 여러 테이크 반복(8초 간격)
```

**옵션**

| 옵션 | 기본 | 설명 |
|---|---|---|
| `--host` | `localhost:8001` | 콘솔 주소 |
| `--boss` | 이름순 첫 로봇 | **안 비키는** 로봇 지정 (예: `--boss pinky2`) |
| `--a` `--b` | 상단 통로 좌/우 자동감지(v0·v3) | 복도 양 끝 정점 수동 지정 |
| `--loop` | off | 씬 무한 반복 |

---

## 5. 씬 1 이 하는 일 (단계별)

```
① reset        전원 IDLE → 순회 랜덤성 제거 (내가 명령한 것만 움직임)
② park_extras  무관한 3번째 로봇을 코너 v7로 치움 (경로 간섭 제거)
③ stage        boss→v0, other→v3 배치 후 *도착까지 폴링 대기*
④ cross        boss: v0→v3 [CHARGE 최우선] · other: v3→v0
                → 상단서 마주침 → 사이클 → other(낮은 tier)가 DEADLOCK
                → 우회 경로 v?→v6→v5→v4→v0 (v5→v4 통과) · boss는 직진
```

**화면에서 볼 것**: 두 로봇이 상단 복도 중앙에서 마주치면 잠깐 두 노드가 펄스 →
`other`가 아래 아일로 방향을 틀어 우회하고, `boss`는 그대로 직진 통과. 태스크 피드엔
`EXECUTING`, fleet 로그엔 `⚠ 교착 감지 → 우회경로` / `⏸ 양보 대기`가 뜬다.

---

## 6. 트러블슈팅

| 증상 | 원인 / 조치 |
|---|---|
| `콘솔 연결 실패` | `./run_sim.sh`로 스택부터 기동. `--host`로 주소 확인 |
| `로봇이 2대 이상 필요` | sim에 slotcar가 안 뜸 → gazebo 창 로그 확인 |
| 도착 타임아웃(45s) 경고 | 로봇 스폰 위치/속도 차이 → 계속 진행됨. `--a/--b`로 더 가까운 복도 지정 |
| 우회 안 하고 그냥 멈춤 | 상대가 *움직이지 않아* 사이클 미형성 → 씬 1(head-on)이나 씬 2(STOP) 사용 |
| 3번째 로봇이 경로 막음 | `park_extras`가 v7로 치우지만, 겹치면 `PARK` 상수(스크립트 상단) 변경 |

> 실행 로그를 붙이면 도착 임계값·정점 파라미터를 바로 튜닝할 수 있다.

---

## 7. (선택) 자막 오버레이

녹화용으로 콘솔 화면에 "P2 양보 → 우회" 배너를 띄우려면 `service/aba_service/aba_service/console.py`
의 HTML에 배너 한 줄을 추가하면 된다 (양보 이벤트를 `/api/state`로 노출 → 상단바에 표시).
필요 시 별도 요청.
